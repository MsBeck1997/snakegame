// sets canvas on index to 'canvas', then sets context to '2d' to render
const canvas = document.getElementById('snake')
const canvasContext = canvas.getContext('2d')

// set a 'unit' for measurement game-wide
const box = 32px
